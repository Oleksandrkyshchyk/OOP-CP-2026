<Window x:Class="Carpooling.UI.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизація - Carpooling" Height="430" Width="400" 
        WindowStartupLocation="CenterScreen" Background="#F5F5F5">

    <Window.Resources>
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="8" 
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1976D2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LinkButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#2196F3"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <TextBlock x:Name="text" 
                                   Text="{TemplateBinding Content}" 
                                   Background="Transparent"
                                   HorizontalAlignment="Center" 
                                   VerticalAlignment="Center"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="text" Property="TextDecorations" Value="Underline"/>
                                <Setter TargetName="text" Property="Foreground" Value="#1976D2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GuestButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#757575"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <TextBlock x:Name="text" 
                                   Text="{TemplateBinding Content}" 
                                   FontStyle="Italic"
                                   HorizontalAlignment="Center" 
                                   VerticalAlignment="Center"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="text" Property="Foreground" Value="#333"/>
                                <Setter TargetName="text" Property="TextDecorations" Value="Underline"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="30">
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="Вхід у систему" FontSize="26" FontWeight="Bold" 
                       HorizontalAlignment="Center" Margin="0,0,0,25" Foreground="#333"/>

            <TextBlock Text="Логін:" Foreground="#555" Margin="0,0,0,5"/>
            <TextBox x:Name="txtLogin" Height="34" Margin="0,0,0,12" VerticalContentAlignment="Center" Padding="8,0"/>

            <TextBlock Text="Пароль:" Foreground="#555" Margin="0,0,0,5"/>
            <PasswordBox x:Name="txtPassword" Height="34" Margin="0,0,0,20" VerticalContentAlignment="Center" Padding="8,0"/>

            <Button Content="Увійти" Height="42" 
                    Style="{StaticResource PrimaryButtonStyle}"
                    Click="btnLogin_Click"/>

            <Button Content="Створити новий акаунт" Margin="0,15,0,0" 
                    Style="{StaticResource LinkButtonStyle}"
                    Cursor="Hand" Click="btnRegister_Click"/>

            <Grid Margin="0,20,0,10">
                <Separator VerticalAlignment="Center" Foreground="#DDD"/>
                <TextBlock Text="або" HorizontalAlignment="Center" Background="#F5F5F5" 
                           Padding="10,0" Foreground="#AAA" FontSize="11"/>
            </Grid>

            <Button Content="Продовжити як гість" 
                    Style="{StaticResource GuestButtonStyle}"
                    Cursor="Hand" Click="btnGuest_Click"/>
        </StackPanel>
    </Grid>
</Window>