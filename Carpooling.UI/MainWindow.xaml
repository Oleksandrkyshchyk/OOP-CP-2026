<Window x:Class="Carpooling.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FocusFlow Carpooling - Головна" Height="620" Width="950"
        WindowStartupLocation="CenterScreen" Background="#F4F7F9">

    <Window.Resources>
        <Style x:Key="LinkButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#2196F3"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <TextBlock x:Name="text" 
                                   Text="{TemplateBinding Content}" 
                                   Background="Transparent"
                                   HorizontalAlignment="Center" 
                                   VerticalAlignment="Center"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="text" Property="TextDecorations" Value="Underline"/>
                                <Setter TargetName="text" Property="Foreground" Value="#1976D2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" CornerRadius="5" BorderBrush="#CCC" BorderThickness="1" Background="White">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#2196F3"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="6*"/>
            <ColumnDefinition Width="19*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Grid.ColumnSpan="2">
            <DockPanel LastChildFill="False" Margin="30,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Carpooling.App" FontSize="26" FontWeight="Bold" Foreground="#2D3436" Margin="8,0,0,0"/>
                </StackPanel>

                <Button x:Name="btnAccount" DockPanel.Dock="Right" Content="Мій профіль" 
                        Style="{StaticResource LinkButtonStyle}" VerticalAlignment="Center"
                        Click="btnAccount_Click"/>
            </DockPanel>
        </Border>

        <Grid Grid.Row="1" Margin="40,40,40,40" Grid.ColumnSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="380"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="15" Padding="25" BorderBrush="#EEE" BorderThickness="1">
                <StackPanel VerticalAlignment="Top">
                    <TextBlock Text="Куди їдемо сьогодні?" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,25" Foreground="#2D3436"/>

                    <TextBlock Text="Звідки:" Margin="0,0,0,5" Foreground="#636E72"/>
                    <TextBox x:Name="txtFrom" Margin="0,0,0,15"/>

                    <TextBlock Text="Куди:" Margin="0,0,0,5" Foreground="#636E72"/>
                    <TextBox x:Name="txtTo" Margin="0,0,0,15"/>

                    <TextBlock Text="Дата:" Margin="0,0,0,5" Foreground="#636E72"/>
                    <DatePicker x:Name="dateTrip" Height="35" VerticalContentAlignment="Center" Margin="0,0,0,25"/>

                    <Button x:Name="btnSearch" Content="Знайти поїздку" 
                            Height="45" Background="#0984E3" Foreground="White" FontSize="16" FontWeight="Bold"
                            Click="btnSearch_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="btnBorder" Background="{TemplateBinding Background}" CornerRadius="8">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="btnBorder" Property="Background" Value="#1976D2"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <Button x:Name="btnCreateTrip" Content="Створити поїздку" 
                            Height="45" Margin="0,15,0,0" Background="#27AE60" Foreground="White" FontSize="16" FontWeight="Bold"
                            Visibility="Collapsed"
                            Click="btnCreateTrip_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="btnBorder" Background="{TemplateBinding Background}" CornerRadius="8">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="btnBorder" Property="Background" Value="#219150"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <Button x:Name="btnAdminPanel" Content="Панель адміністратора" 
                            Height="45" Margin="0,15,0,0" Background="#1976D2" Foreground="White" FontSize="16" FontWeight="Bold"
                            Visibility="Collapsed"
                            Click="btnAdminPanel_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="btnBorder" Background="{TemplateBinding Background}" CornerRadius="8">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="btnBorder" Property="Background" Value="#0D47A1"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Margin="40,0,0,0">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="Гарної дороги!" FontSize="42" FontWeight="Bold" Foreground="#2D3436" HorizontalAlignment="Center"/>
                    <TextBlock Text="Знайдіть надійних попутників за лічені хвилини." 
                               FontSize="18" Foreground="#636E72" Margin="0,15" HorizontalAlignment="Center" TextAlignment="Center"/>
                    <Rectangle Width="100" Height="4" Fill="#2196F3" RadiusX="2" RadiusY="2" Margin="0,20"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>