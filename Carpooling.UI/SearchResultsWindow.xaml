<Window x:Class="Carpooling.UI.SearchResultsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Результати пошуку - Carpooling.App" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen" Background="#F4F7F9">

    <Window.Resources>
        <Style x:Key="LinkButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#2196F3"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <TextBlock x:Name="text" 
                                   Text="{TemplateBinding Content}" 
                                   Background="Transparent"
                                   HorizontalAlignment="Center" 
                                   VerticalAlignment="Center"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="text" Property="TextDecorations" Value="Underline"/>
                                <Setter TargetName="text" Property="Foreground" Value="#1976D2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" CornerRadius="5" BorderBrush="#CCC" BorderThickness="1" Background="White">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#2196F3"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <DockPanel LastChildFill="False" Margin="30,0">
                <TextBlock Text="Carpooling.App" FontSize="26" FontWeight="Bold" Foreground="#2D3436" VerticalAlignment="Center"/>
                <Button x:Name="btnAccount" DockPanel.Dock="Right" Content="Мій профіль" 
                        Style="{StaticResource LinkButtonStyle}" VerticalAlignment="Center"
                        Click="btnAccount_Click"/>
            </DockPanel>
        </Border>

        <Border Grid.Row="1" Background="White" Margin="0,0,0,10" Padding="30,15" BorderBrush="#EEE" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <StackPanel Margin="0,0,15,0">
                    <TextBlock Text="Звідки:" FontSize="12" Foreground="#636E72" Margin="0,0,0,3"/>
                    <TextBox x:Name="txtFrom" Width="180"/>
                </StackPanel>

                <StackPanel Margin="0,0,15,0">
                    <TextBlock Text="Куди:" FontSize="12" Foreground="#636E72" Margin="0,0,0,3"/>
                    <TextBox x:Name="txtTo" Width="180"/>
                </StackPanel>

                <StackPanel Margin="0,0,15,0">
                    <TextBlock Text="Дата:" FontSize="12" Foreground="#636E72" Margin="0,0,0,3"/>
                    <DatePicker x:Name="dateTrip" Height="35" Width="150" VerticalContentAlignment="Center"/>
                </StackPanel>

                <Button x:Name="btnUpdateSearch" Content="Оновити" Width="120" Height="35" 
                        Margin="0,15,0,0" Background="#0984E3" Foreground="White" FontWeight="Bold"
                        Click="btnUpdateSearch_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="btnBorder" Background="{TemplateBinding Background}" CornerRadius="5">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="btnBorder" Property="Background" Value="#1976D2"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="30,10">
                <TextBlock x:Name="txtResultSummary" Text="Знайдені поїздки за вашим запитом:" 
                           FontSize="18" FontWeight="SemiBold" Foreground="#2D3436" Margin="0,0,0,15"/>

                <ListBox x:Name="lstTrips" Background="Transparent" BorderThickness="0" 
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" CornerRadius="10" Margin="0,0,0,15" Padding="20" 
                                    BorderBrush="#EEE" BorderThickness="1" Width="900">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel VerticalAlignment="Center" Margin="0,0,30,0">
                                        <TextBlock Text="{Binding DepartureTime, StringFormat='{}{0:HH:mm}'}" FontSize="24" FontWeight="Bold" Foreground="#2D3436"/>
                                        <TextBlock Text="{Binding DepartureTime, StringFormat='{}{0:dd.MM.yyyy}'}" FontSize="14" Foreground="#636E72"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                                        <TextBlock Text="{Binding Price, StringFormat='{}{0} грн'}" 
               FontSize="22" FontWeight="Bold" Foreground="#00B894" HorizontalAlignment="Right"/>
                                        <TextBlock Text="{Binding TotalSeats, StringFormat='{}Місць: {0}'}" 
               FontSize="12" Foreground="#636E72" HorizontalAlignment="Right" Margin="0,2,0,8"/>
                                        <Button Content="Забронювати" Width="120" Height="35" 
            Background="#0984E3" Foreground="White" FontWeight="Bold"
            Tag="{Binding}" Click="btnBook_Click">
                                            <Button.Template>
                                                <ControlTemplate TargetType="Button">
                                                    <Border x:Name="btnBorder" Background="{TemplateBinding Background}" CornerRadius="5">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="btnBorder" Property="Background" Value="#1976D2"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Button.Template>
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <TextBlock x:Name="txtNoResults" Text="На жаль, поїздок за таким маршрутом не знайдено." 
                           HorizontalAlignment="Center" Margin="0,50,0,0" FontSize="16" Foreground="#636E72" Visibility="Collapsed"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>